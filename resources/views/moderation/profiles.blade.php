<x-layouts.app><h2>{{ __('wishubest.directory.review') }}</h2>@forelse($profiles as $profile)<article><h3>{{ $profile->display_name }}</h3><form method="post" action="{{ route('moderation.profiles.approve',$profile) }}">@csrf<button>{{ __('wishubest.directory.approve') }}</button></form><form method="post" action="{{ route('moderation.profiles.reject',$profile) }}">@csrf<label>Reason <input name="reason"></label><button>{{ __('wishubest.directory.reject') }}</button></form></article>@empty<p>{{ __('wishubest.directory.no_profiles') }}</p>@endforelse</x-layouts.app>
